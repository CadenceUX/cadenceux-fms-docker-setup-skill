{
  "cases": [
    {
      "prompt": "I've downloaded FileMaker Server 26 and want to install it on Docker Desktop on my Mac.",
      "expected": "Triggers the skill; follows the pre-flight checks, builds from Claris's Dockerfile (not fms_Docker_Installer.sh), pins ubuntu:24.04, creates named volumes, runs the container, and walks through the assisted-install wizard."
    },
    {
      "prompt": "FileMaker Server's Admin Console is showing 89% disk usage but I don't think that's right.",
      "expected": "Triggers the skill via the disk-usage symptom trigger; identifies a bind-mounted install folder as the likely cause and points to the troubleshooting reference's disk-usage section rather than guessing."
    },
    {
      "prompt": "I ran docker rm on my FMS container and now fmsadmin says command not found, but the databases still show up in the volume.",
      "expected": "Triggers the skill via the 'FMS software disappearing after a container was recreated' symptom. Rules out the wrong shell, then checks docker images fmsdocker for a committed image and recreates from it (settings recorded, confirmation first). Only if no image exists: backs up the four volumes, reinstalls the same build the data last ran (from Event.log) with docker exec -it and 'load previous configuration', and cleans up before committing."
    },
    {
      "prompt": "Claris released a FileMaker Server update overnight. My FMS runs in Docker — can you update it?",
      "expected": "Triggers the skill; runs the staleness check against Claris's updater feed and the container's dpkg version; asks the developer for the licence-linked Linux zip rather than downloading it; then follows U1–U7: diff the packages, snapshot to a dated pre-upgrade tag, stop the container and back up all four volumes, dry-run the .deb, hand the interactive install to a named terminal tab with the licence prompt left to the developer, verify, delete the .deb before committing. On 26.0.3+ does not re-apply the Step 6 Nginx patch without asking."
    },
    {
      "prompt": "I tried installing FMS in Docker last month and gave up. Can you set it up properly from scratch now?",
      "expected": "Triggers the skill. Pre-flight check 7 finds leftover fms-* volumes, an old container or fmsdocker images, and explains that docker volume create would silently reuse old data. Asks whether to reuse, rename or back up and remove, and removes nothing without an explicit yes. Confirms the real container name, and fills Step 3's MEM, CPUS and port placeholders from the pre-flight results instead of fixed values."
    }
  ],
  "near_misses": [
    {
      "prompt": "Can you write me a FileMaker script that finds all overdue invoices?",
      "expected_to_trigger": false,
      "why": "FileMaker Pro script authoring, not server/Docker infrastructure — should route to fmp-dev-* skills instead."
    },
    {
      "prompt": "How do I set up a Windows Server VM to host FileMaker Server?",
      "expected_to_trigger": false,
      "why": "Not Docker-related; this skill is specifically Docker-container installs, not general Windows/VM hosting."
    }
  ]
}
